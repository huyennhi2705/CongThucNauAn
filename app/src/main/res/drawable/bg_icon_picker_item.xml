<?xml version="1.0" encoding="utf-8"?>
<!--
    Drawable selector cho từng ô icon trong picker.
    - Khi selected=true  → viền cam đậm, nền cam nhạt
    - Khi selected=false → nền trắng, viền xám nhạt
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Trạng thái đang được chọn -->
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <solid android:color="#FFE0C8" />
            <stroke android:width="2dp" android:color="#E8631A" />
            <corners android:radius="12dp" />
        </shape>
    </item>

    <!-- Trạng thái bình thường -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" />
            <stroke android:width="1dp" android:color="#E0E0E0" />
            <corners android:radius="12dp" />
        </shape>
    </item>

</selector>